---
change_id: real-board-over-ble
title: Real board over BLE
status: impl_reviewed
created: 2026-06-29
updated: 2026-07-01
archived_at: null
---

## Notes

<!-- Free-form notes for this change: links, ad-hoc context, decisions that don't belong in research/frame/plan. -->

- 2026-07-04: **F4 closed; slice being archived.** Filled the `manual-verification.md` §4/§5 result logs (device/OS/date + the accepted flaky-connect caveat) and ticked §Status, recording the 2026-06-30 on-hardware acceptance; the p8 SHA `f41f766` was already written back on Progress rows 8.1–8.6. Per the user's decision the remaining impl-review findings — **F1** (firmware `sm_bonding=1` half-revert), **F2** (plan.md/plan-brief still describe forcing encryption, incl. the stale "Encrypted bond persists" wording on Progress row 8.5), **F3** (`firmware/AGENTS.md` encrypted-bond claim), **F5** (connect/reconnect state asymmetry), **F6** (stale "encryption-gated" comments) — are **deferred to roadmap S-10** (`ble-connectivity-robustness`), not fixed here. Note: `/10x-archive` freezes `plan.md`, so its planning-time encryption prose stays frozen; the authoritative surfaces (`lessons.md`, this `change.md`, `contract-surfaces.md`, firmware) already reflect the plaintext revert.

- 2026-07-01: **`/10x-impl-review` on the full slice (all 8 phases) → verdict NEEDS ATTENTION** (0 critical, 4 warnings, 2 observations; all decisions PENDING). Automated criteria re-run live this review, all green (`testAndroidHostTest`, `iosSimulatorArm64Test`, `wasmJsTest`, ktlint source-clean, firmware `pio test` 15/15 + `pio run`). Code is faithful to plan intent across all phases; all guardrails held; core invariants verified. Findings cluster on the encryption→plaintext revert not reaching every surface + the manual-gate evidence trail: **F1** firmware half-revert (`sm_bonding=1`/key-dist + a contradictory `:459-460` comment remain despite plaintext chars — REPEAT_PAIRING handler is the recovery net, keep it; sm_bonding=0 decision owned by S-10); **F2** plan.md/plan-brief.md still describe forcing encryption; **F3** `firmware/AGENTS.md:11` asserts the encrypted-bond delta, omits the revert; **F4** Phase 8 Progress rows `[x]` without SHA write-back (`f41f766`) and manual-verification.md result logs left blank; **F5** connect()/reconnect() state-mgmt asymmetries (minor); **F6** stale "encryption-gated" code comments. Report: `reviews/impl-review.md`. Chosen: **save & triage later** — resume with `/10x-impl-review context/changes/real-board-over-ble/reviews/impl-review.md`. Recommend closing F4 (SHA write-back + fill the gate log) before `/10x-archive`.
- 2026-06-30: **S-09 ACCEPTED / slice closed.** User ran the on-hardware gate on the real board (`SmartChessboard-DA3A`): full game (moves, capture, promotion, castling, both side buttons), the live reed-matrix overlay, diagnostic mode, and resume all work as expected. Manual Progress rows (3.5 / 4.5 / 5.4–5.6 / 6.5 / 7.5 / 8.3–8.6) ticked. **Accepted with one documented caveat:** raw BLE connect/disconnect/reconnect stability was flaky on the test Android tablet (possibly device-specific — that tablet's BT). The encryption→plaintext revert is **provisional**; the pairing model + connectivity robustness need multi-device re-testing → recorded as roadmap **S-10** (`ble-connectivity-robustness`) + a `lessons.md` entry ("force-encryption-for-iOS-bonding backfired → plaintext MVP posture"). Minor UI-polish items the user will do later are noted under roadmap **Parked**. Next: `/10x-impl-review` then `/10x-archive`.
- 2026-06-30: **Phase 8 (on-hardware acceptance gate).** `manual-verification.md` written (app-driven gate, both platforms; absorbs the deferred device-manual rows 3.5/4.5/5.4–5.6/6.5/7.5). Automated 8.1/8.2 green (3 targets + ktlint; firmware `pio test -e native` 15/15 + `pio run`). The gate surfaced real defects, all fixed **in-slice (uncommitted, working tree)**: (1) **foreground auto-reconnect + manual "Reconnect" button** — Kable `observe()` re-subscribes but never *initiates* a reconnect, so a drop left the play screen stuck; added a bounded-backoff loop in both `KableBoardAdapter`s (gated on `connectedOnce`) + `BoardTransport.reconnect()` wired through `PhysicalPlayViewModel` to a banner button; (2) **keep-screen-on** via Compose's common `Modifier.keepScreenOn()` (a manual iOS `UIApplication.idleTimerDisabled` was clobbered by CMP's own `UIKitIdleTimerManager`; my first `expect/actual` was wrong); (3) **illegal-move banner** now shows the rejection reason over the generic "Set up the board" text; (4) **BT-off crash** — the scan collect is wrapped → `BluetoothOff` state instead of an uncaught throw; (5) **stuck/first-pair connect** — `connect()` retries internally + defers the Kable-state mirror, with a 30 s `withTimeoutOrNull` so it can't hang on "Connecting…" forever (falls to Retry). **(6) MAJOR DECISION — encryption reverted to plaintext (user-approved).** The recurring `reason=531` (HCI 0x13) immediate-drops on iOS were the **Phase 2 `_ENC` requirement** forcing an unreliable iOS bond (stale-LTK desync needing a manual "Forget" every time; peripheral can't force a central to drop its bond). For a dumb-sensor board with a minimal threat model, reverted `ble_service.cpp` to plaintext `NOTIFY`/`WRITE` (no `_ENC`) + updated `contract-surfaces.md` §1.1/§1.2/§1.8 + `prd-firmware.md` (FR-FW-004/Out-of-MVP); F-03 worked plaintext with nRF Connect, so this is the proven posture. Encryption + authenticated pairing → post-MVP. **Recovery for an already-bonded device:** re-flash plaintext (`pio run -t erase && pio run -t upload`) + "Forget" the board in the phone's system Bluetooth once. Manual gate (8.3–8.6 + the deferred rows) still pending the on-hardware re-run on **both** platforms; the Phase 8 commit is held until it passes.
- 2026-06-29: `research.md` written (5 parallel agents). S-09 is a transport-fill — the `BoardConnection` port, `BoardWireCodec`, and the `SnapshotReceived` reconnect seam already exist; firmware F-03 is on-hardware-verified. Library recommendation: **Kable-first**. FR-012 reconnect/reconcile is **in scope**.
- 2026-06-29: decisions locked — iOS bonding **B (force encryption → firmware+contract+re-verify delta; S-09 not mobile-only)**; connection UX **full MVI screen**; iOS background **foreground-first**; hardware **ready, ADC buttons = MVP target**; emulator **test-only/no UI, device uses the real board**. Kotlin 2.4.0 confirmed; Blue-Falcon evaluated & rejected (no iOS state-restoration). Next: `/10x-plan real-board-over-ble`.
- 2026-06-29: `/10x-plan-review` (deep) → verdict **REVISE (light) → SOUND after triage**. Grounding 15/15 paths, every load-bearing claim verified to the line. 4 findings, all fixed in `plan.md`: **F1** (Phase 2) NimBLE notify-CCCD encryption under-specified → gate the bond via `mobile_command` `WRITE_ENC` (REQUEST_SNAPSHOT-on-connect forces it), CCCD subscribe best-effort + nRF-verified; **F2** (Phase 5) add the mandatory `wasmJsMain` `actual` for the permission `expect`; **F3** (Phase 4) adapter constructed idle (no emulator-style connect-on-bind); **F4** reworded the reducer "TODO(S-09)" breadcrumb (it's a comment, not a marker). Report: `reviews/plan-review.md`. Next: `/10x-implement real-board-over-ble phase 1`.
- 2026-06-30: Phase 2 implemented + on-hardware verified (board `SmartChessboard-DA3A`, nRF Connect). Both characteristics now require an encrypted link: `mobile_command` write via `BLE_GATT_CHR_F_WRITE_ENC`, `board_event` subscribe via `BLE_GATT_CHR_F_NOTIFY_INDICATE_ENC` (this NimBLE build propagates the flag to the auto-CCCD's write permission — confirmed by reading `ble_gatts.c`; `min_key_size` does **not** reach the CCCD here, so the flag is the lever). Manual gate 2.3/2.4 result: an unbonded **write triggers pairing+encryption** (load-bearing, ✓), the **subscribe also triggers it** (✓ — the CCCD gate works, better than the plan's best-effort hedge), and the **bond persists across reconnect** without re-pairing (Just-Works, no PIN, ✓). Contract §1.1/§1.2/§1.8 + `prd-firmware.md` FR-FW-004/Out-of-MVP updated to record the encrypted-but-unauthenticated link. `pio test -e native` 15/15 + `pio run` green.
- 2026-06-30: Phase 4 (DI swap + teardown) implemented (`b450c13`). `PlatformModule.android.kt`/`.ios.kt` drop `EmulatedBoard` from production DI (now test-only — still constructed directly by every physical-flow test) and bind the real `KableBoardAdapter` as **one instance under two types** via `binds(arrayOf(BoardConnection::class, BoardTransport::class))`. Adapter is constructed **idle** (no emulator-style connect-on-bind). The `TODO(S-09)` leak is fixed via Koin `onClose { it?.close() }`. Two adaptations vs the plan's literal contract (no contract change): (a) the plan's `bind … bind …` chain narrows `KoinDefinition`'s type param to the first bound type, breaking the second `bind` and the `onClose` lambda's receiver → switched to `binds(arrayOf(...))` with `onClose` placed **before** it so `it` stays `KableBoardAdapter`; (b) Koin `onClose` is non-suspend but the adapter's teardown is suspend → added a non-suspend `close()` to `BleBoardAdapterCore` that launches `disconnect()` on the adapter's own scope and cancels it in `finally` (so the eager `connectionState` `stateIn` collector dies with the graph), and removed the now-redundant per-platform `suspend fun teardown()` (merged into `close()`). Automated gates 4.1–4.4 green: `:shared:assemble`, `testAndroidHostTest` + `iosSimulatorArm64Test`, `wasmJsTest`, ktlint. Manual 4.5 stays deferred to the Phase 8 hardware gate.
- 2026-06-30: Phase 3 (BLE adapter) implemented. New `data/board/ble/` behind the existing `BoardConnection` port: a `BoardTransport` driver surface (`DiscoveredBoard`/`BoardTransportState`); a pure `BleMapping` seam (`notify → BoardEvent` dropping malformed + `connectionState` derivation); `BleBoardAdapterCore` (commonMain, no-Kable flow plumbing + `send()` guard); `KableBoardAdapter` in androidMain **and** iosMain (scan by service-UUID, connect, `observe → decode`, `write WithResponse`, Kable-state mapping, teardown). Two Kable-0.43.1 realities adapted (no contract change): `characteristicOf(String, String)` is **deprecated-to-error** → used the `Uuid` overload; `Advertisement.identifier` is `String` on Android but `Uuid` on iOS → normalized to `String` (the single spot the two adapters diverge). Seam carries the tests: `BleMappingTest` 5/5 green on JVM (`testAndroidHostTest`) **and** Native (`iosSimulatorArm64Test`); wasm unaffected; ktlint clean. Manual 3.5 stays deferred to the Phase 8 hardware gate (no real radio off-device).
- 2026-06-30: Phase 5 (connection screen MVI + permissions) implemented. New `presentation/connection/` MVI core: `ConnectionUiState` (sealed `ConnectionPhase` + cross-cutting `devices`/`rememberedBoardId`/`connectingTo`), pure `reduceConnection` (permission gate → scan → select/auto-connect → pair → connected, full `ConnectionFailure` taxonomy + forget/retry), `ConnectionViewModel` (collects `BoardTransport.transportState`/`scanResults`, interprets effects; teardown stops scan but **never disconnects** so the live link persists into physical play, where `PhysicalPlayViewModel` re-requests the snapshot on the CONNECTED it sees — the idempotent duplicate-snapshot), and `ConnectionScreen`. New `RememberedBoardStore` (domain) + `SettingsRememberedBoardStore` (`ble.`-prefixed key) back the remembered-device auto-connect. `BlePermissionController` `expect` + Android (runtime `BLUETOOTH_SCAN`/`CONNECT` API 31+ / `ACCESS_FINE_LOCATION` ≤30 via an Activity-result launcher) / iOS (OS prompts on first `CBCentralManager` use, gated by the Info.plist string) / wasm (no-op `Unsupported` stub — the mandatory F2 `actual`) `actual`s. Nav: new `@Serializable ConnectionKey(gameId)` (registered polymorphically) gates physical play — both New-game-create and History-resume now route physical → `ConnectionKey` → (connected) → `PhysicalPlayKey` (replace, so Back returns to History). Android manifest BLE perms (`neverForLocation`, legacy ≤30 set, optional `uses-feature`); iOS `NSBluetoothAlwaysUsageDescription`. Three adaptations vs the plan's literal text (no contract change): (a) the manifest lives in `androidApp/`, not the plan's `composeApp/`; (b) auto-connect **scans first** then auto-selects the remembered board the moment it reappears (the Kable adapter builds a peripheral only from a live advertisement, so a blind `connect(id)` would fail); (c) `androidx.activity.compose` added to `androidMain` for the permission launcher. Automated gates 5.1–5.3 green: `testAndroidHostTest` + `iosSimulatorArm64Test` (incl. new `ConnectionReducerTest` 20 cases + `ConnectionViewModelTest` 5 cases), `wasmJsTest` + `:webApp` distribution, ktlint. Manual 5.4–5.6 (device permission prompts, forget/re-pair) deferred to the Phase 8 on-device pass.
- 2026-06-30: Phase 6 (reconnect-reconcile reducer flag, FR-012) implemented on a parallel worktree (`funny-einstein-604879`, branch `impl/s09-phase6-reconnect-reconcile`, feat `0e604ca`). New `reconnectReconciling` gate armed in the `BoardConnected` arm, cleared by the shared `SnapshotReceived` at-rest board-match seam (parallel to S-08 `awaitingResumeConfirm`); added to `acceptanceBlocked`, the `accumulate()` guard, the `confirm()` gate, and the `commit()` reset. `PhysicalPlayReducerTest` +8 cases; new `PhysicalReconnectEndToEndTest` (match + offline-change-to-restore, `runCurrent`-discipline); `PhysicalRecoverEndToEndTest` reworked to inject INCONSISTENT over a continuous connection (the reconnect gate supersedes the old disconnect-reconnect injection). Gates 6.1–6.4 green.
- 2026-06-30: Phase 7 (live matrix overlay) implemented on the same parallel worktree (feat `c7d6132`). Display-only `sensedOccupancy: Long?` on `Playing` (separate from `latestOccupancy`, never read by any gate): reset on `SnapshotReceived`, clear-on-lift / set-on-place via an h8-safe helper, threaded so the overlay tracks the board even while a gate holds. `ChessBoardView` gains an optional `occupancyDots: Long? = null` corner-dot overlay (null default keeps Replay/Play/web unchanged); `ChessColors` gains a constant-across-modes `occupancyDot` token; `PhysicalPlayScreen` wires it with an on-by-default "Sensor dots" toggle. Gates 7.1–7.4 green.
- 2026-06-30: Merged the p6+p7 branch into the p2–p5 line (`claude/nifty-napier-4e2cf4`, merge `1cab730`). The p6/p7 branch was cut at p1, so it carried no p2–p5 work and p2–p5 never touched the physical reducer/contract/screen/board files — the only overlap was `plan.md` (Progress), which git auto-merged into the correct 1.x–7.x union. Post-merge full-suite re-verify green on **all three targets** (`testAndroidHostTest`, `iosSimulatorArm64Test`, `wasmJsTest`) + ktlint. Remaining: Phase 8 (hard on-hardware acceptance gate) + the deferred device-manual rows (3.5, 4.5, 5.4–5.6, 6.5, 7.5).
- 2026-06-30: `/10x-impl-review` on **Phases 6 & 7** (run on the parallel worktree before the merge) → verdict **APPROVED** (0 critical, 0 warnings, 3 non-blocking observations, all decisions PENDING): F1 the recover E2E was *modified* not just extended (coverage preserved in the reducer suite), F2 the `OccupancyDot` colour is a placeholder to tune on real reeds in Phase 8, F3 the "Sensor dots" toggle is screen-local (persist via `multiplatform-settings` post-MVP only if cross-session memory is wanted — same store as the remembered board id). Report preserved at `reviews/impl-review-phase-6-7.md` (rescued from the now-removed `funny-einstein-604879` worktree, which had it uncommitted).
- 2026-06-29: `plan.md` + `plan-brief.md` written (8 phases). Planning decisions on top of research: firmware encryption delta rides **inside S-09**; adapter is **one object** implementing the `BoardConnection` port + a new `BoardTransport` driver surface (mirrors `EmulatedBoard`); connection screen scope **richer** (RSSI/forget-repair/manual-retry/auto-connect/full error taxonomy); on-hardware acceptance is a **hard blocking phase** on Android **and** iOS. Added a feature beyond research at user request: a **live reed-matrix overlay** on the play board — a new display-only `sensedOccupancy` field (snapshot-reset, event-folded), plain always-on corner dots (toggle), zero extra BLE. Next: `/10x-plan-review real-board-over-ble` then `/10x-implement real-board-over-ble phase 1`.
